# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# Model weights and vendored runtimes (tens of GB, fetched separately)
pipeline/qwen/models/
pipeline/sam3/weights/
pipeline/tapnext-main/
runtime/

# Bot output
out/
_batches/
*.log

# Local rollback copies (same content is already in git history)
backup/

# Generated bench shots (rendered frames + exports; rebuilt by bench/make_synth.py)
bench/synth/

# Vendored third-party trackers cloned for evaluation (MFT is CC BY-NC-SA, NonCommercial)
experiments/*/vendor/

# Blender_assitant: downloaded runtime, weights and vendored tapnet (Apache-2.0)
# `runtime/` above is a bare pattern and already covers Blender_assitant/runtime/.
Blender_assitant/weights/
Blender_assitant/vendor/
Blender_assitant/dist/
Blender_assitant/logs/
Blender_assitant/config/paths.json
